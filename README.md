Tool to plot the probabilities of obtaining diffent results in Savage Worlds with different dice.

Dependencies: `matplotlib` (and `python3`)

By default, it will create a bunch of `.png` files in the current folder.
